# Dependencias para el BANCO DE PRUEBAS y los tests.
#
# A propósito NO incluye requirements.txt: la app de escritorio depende de
# pycaw/comtypes (solo Windows) y PyQt6, y el banco no necesita nada de eso.
# Así corre igual en la Pi, en un contenedor o en CI.
#
#   pip install -r requirements-dev.txt
#   pytest
#   python benchmark/bench_dictation.py

faster-whisper
numpy
pytest

# Solo para las herramientas que usan el micrófono o el teclado:
# benchmark/record_clip.py (grabar clips propios) y streaming_prototype.py
# (push-to-talk en vivo). Si vas a correr el banco sobre clips ya grabados,
# no hace falta ninguna de las dos.
#
# OJO en Linux: pynput necesita X11 o permisos de uinput. En una Pi headless
# por SSH el listener de teclado no arranca — ahí usá bench_dictation.py, que
# no necesita ni micrófono ni teclado ni pantalla.
sounddevice
pynput
